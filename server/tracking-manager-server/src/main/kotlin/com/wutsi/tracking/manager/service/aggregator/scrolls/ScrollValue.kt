package com.wutsi.tracking.manager.service.aggregator.scrolls

import com.wutsi.tracking.manager.service.aggregator.KeyPair

class ScrollValue(key: ScrollKey, value: Long) : KeyPair<ScrollKey, Long>(key, value)
