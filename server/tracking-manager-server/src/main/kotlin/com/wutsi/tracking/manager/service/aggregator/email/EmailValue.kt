package com.wutsi.tracking.manager.service.aggregator.email

import com.wutsi.tracking.manager.service.aggregator.KeyPair

class EmailValue(key: EmailKey, value: Long) : KeyPair<EmailKey, Long>(key, value)
