package com.wutsi.tracking.manager.service.aggregator.reads

import com.wutsi.tracking.manager.service.aggregator.KeyPair

class StoryRead(key: String, value: Long) : KeyPair<String, Long>(key, value)
